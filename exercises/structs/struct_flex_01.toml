common_mistake = "Utiliser memcmp sur des structs non initialisées avec {0} — le padding peut contenir des valeurs aléatoires"
description = """
NSY103 — En C, l'affectation `a = b` copie une struct bit à bit (shallow copy). `memcmp` compare deux structs octet par octet. Attention : le padding entre les champs peut rendre memcmp non fiable si la struct n'est pas initialisée proprement.

Démontrez la copie de struct, la comparaison avec memcmp, et la modification indépendante après copie."""
difficulty = 2
exercise_type = "complete"
hints = [
    "Copie de struct : `Marker b = a;` — copie bit à bit de tous les champs",
    "Après copie, a et b sont indépendants — modifier b ne modifie pas a",
    "`memcmp(&a, &b, sizeof(Marker))` retourne 0 si identiques. Fonctionne car {0} initialise tout (y compris le padding).",
]
id = "struct-flex-01"
kc_ids = ["KC-C03"]
key_concept = "L'affectation de struct copie bit à bit — copie profonde si pas de pointeurs, shallow sinon"
lang = "c"
prerequisites = ["struct-array-01"]
solution_code = '''
#include <stdio.h>
#include <string.h>

typedef struct {
    int x;
    int y;
    char label[16];
} Marker;

Marker make_marker(int x, int y, const char *label) {
    Marker m = {0};
    m.x = x;
    m.y = y;
    strncpy(m.label, label, sizeof(m.label) - 1);
    m.label[sizeof(m.label) - 1] = '\0';
    return m;
}

int main(void) {
    Marker a = make_marker(10, 20, "Alpha");
    Marker b = a;
    printf("a = (%d, %d, %s)\n", a.x, a.y, a.label);
    printf("b = (%d, %d, %s)\n", b.x, b.y, b.label);
    printf("Egaux (memcmp) : %d\n", memcmp(&a, &b, sizeof(Marker)) == 0);

    b.x = 99;
    strncpy(b.label, "Beta", sizeof(b.label) - 1);
    printf("Apres modif b:\n");
    printf("a = (%d, %d, %s)\n", a.x, a.y, a.label);
    printf("b = (%d, %d, %s)\n", b.x, b.y, b.label);
    printf("Egaux (memcmp) : %d\n", memcmp(&a, &b, sizeof(Marker)) == 0);
    return 0;
}'''
starter_code = '''
#include <stdio.h>
#include <string.h>

typedef struct {
    int x;
    int y;
    char label[16];
} Marker;

Marker make_marker(int x, int y, const char *label) {
    Marker m = {0};
    m.x = x;
    m.y = y;
    strncpy(m.label, label, sizeof(m.label) - 1);
    m.label[sizeof(m.label) - 1] = '\0';
    return m;
}

int main(void) {
    Marker a = make_marker(10, 20, "Alpha");
    Marker b = ____;
    printf("a = (%d, %d, %s)\n", a.x, a.y, a.label);
    printf("b = (%d, %d, %s)\n", b.x, b.y, b.label);
    printf("Egaux (memcmp) : %d\n", memcmp(&a, &b, sizeof(Marker)) == 0);

    b.____ = 99;
    strncpy(b.____, "Beta", sizeof(b.label) - 1);
    printf("Apres modif b:\n");
    printf("a = (%d, %d, %s)\n", a.x, a.y, a.label);
    printf("b = (%d, %d, %s)\n", b.x, b.y, b.label);
    printf("Egaux (memcmp) : %d\n", memcmp(&a, &b, sizeof(Marker)) == 0);
    return 0;
}'''
starter_code_stages = [
    '''
#include <stdio.h>
#include <string.h>

/* == EXEMPLE ================================================
 * Copie de struct Vec2 et verification d'independance
 * =========================================================== */
typedef struct { float x, y; } Vec2;

void example_demo(void) {
    Vec2 v1 = {3.0f, 4.0f};
    Vec2 v2 = v1;
    printf("v1 = (%.1f, %.1f)\n", v1.x, v1.y);
    printf("v2 = (%.1f, %.1f)\n", v2.x, v2.y);
    v2.x = 0.0f;
    printf("Apres v2.x=0 : v1.x=%.1f v2.x=%.1f\n", v1.x, v2.x);
    printf("memcmp : %d\n", memcmp(&v1, &v2, sizeof(Vec2)) == 0);
}
/* Resultat: v1=(3.0,4.0) v2=(3.0,4.0) / v1.x=3.0 v2.x=0.0 / memcmp: 0 */

/* == VOTRE EXERCICE =========================================
 * Struct Marker : copie, memcmp, modification independante
 * =========================================================== */
typedef struct { int x; int y; char label[16]; } Marker;

Marker make_marker(int x, int y, const char *label) {
    Marker m = {0};
    m.x = x; m.y = y;
    strncpy(m.label, label, sizeof(m.label) - 1);
    m.label[sizeof(m.label) - 1] = '\0';
    return m;
}

int main(void) {
    /* TODO: créer a, copier dans b, vérifier memcmp, modifier b */
    return 0;
}''',
    '''
#include <stdio.h>
#include <string.h>

typedef struct { int x; int y; char label[16]; } Marker;

Marker make_marker(int x, int y, const char *label) {
    Marker m = {0};
    m.x = x; m.y = y;
    strncpy(m.label, label, sizeof(m.label) - 1);
    m.label[sizeof(m.label) - 1] = '\0';
    return m;
}

int main(void) {
    Marker a = make_marker(10, 20, "Alpha");
    Marker b = a;
    printf("a = (%d, %d, %s)\n", a.x, a.y, a.label);
    printf("b = (%d, %d, %s)\n", b.x, b.y, b.label);
    printf("Egaux (memcmp) : %d\n", memcmp(&a, &b, sizeof(Marker)) == 0);

    b.x = 99;
    strncpy(b.label, "Beta", sizeof(b.label) - 1);
    printf("Apres modif b:\n");
    printf("a = (%d, %d, %s)\n", a.x, a.y, ____);
    printf("b = (%d, %d, %s)\n", b.x, b.y, ____);
    printf("Egaux (memcmp) : %d\n", ____);
    return 0;
}''',
    '''
#include <stdio.h>
#include <string.h>

typedef struct {
    int x;
    int y;
    char label[16];
} Marker;

Marker make_marker(int x, int y, const char *label) {
    Marker m = {0};
    m.x = x;
    m.y = y;
    strncpy(m.label, label, sizeof(m.label) - 1);
    m.label[sizeof(m.label) - 1] = '\0';
    return m;
}

int main(void) {
    Marker a = make_marker(10, 20, "Alpha");
    Marker b = ____;
    printf("a = (%d, %d, %s)\n", a.x, a.y, a.label);
    printf("b = (%d, %d, %s)\n", b.x, b.y, b.label);
    printf("Egaux (memcmp) : %d\n", memcmp(&a, &b, sizeof(Marker)) == 0);

    b.____ = 99;
    strncpy(b.____, "Beta", sizeof(b.label) - 1);
    printf("Apres modif b:\n");
    printf("a = (%d, %d, %s)\n", a.x, a.y, a.label);
    printf("b = (%d, %d, %s)\n", b.x, b.y, b.label);
    printf("Egaux (memcmp) : %d\n", memcmp(&a, &b, sizeof(Marker)) == 0);
    return 0;
}''',
    """
#include <stdio.h>
#include <string.h>

typedef struct { int x; int y; char label[16]; } Marker;

// Créez make_marker, puis démontrez : copie, memcmp, modification
int main(void) {
    // TODO
    return 0;
}""",
    """
#include <stdio.h>
#include <string.h>

typedef struct { int x; int y; char label[16]; } Marker;

int main(void) {
    return 0;
}""",
]
subject = "structs"
title = "Copie de struct et comparaison mémoire"

[validation]
expected_output = """
a = (10, 20, Alpha)
b = (10, 20, Alpha)
Egaux (memcmp) : 1
Apres modif b:
a = (10, 20, Alpha)
b = (99, 20, Beta)
Egaux (memcmp) : 0
"""
mode = "output"

[visualizer]
type = "stack_heap"

[[visualizer.steps]]
arrows = []
explanation = "b = a copie sizeof(Marker) octets de a vers b. Les deux structs sont maintenant identiques mais occupent des emplacements mémoire différents. memcmp retourne 0."
heap = []
label = "Après b = a — copie identique"
step_label = "b = a — copie bit à bit"

[[visualizer.steps.call_frames]]
args = "void"
function_name = "main"
is_active = true

[[visualizer.steps.stack]]
name = "a.x"
value = "10"

[[visualizer.steps.stack]]
name = "a.y"
value = "20"

[[visualizer.steps.stack]]
name = "a.label"
value = '"Alpha"'

[[visualizer.steps.stack]]
name = "b.x"
value = "10"

[[visualizer.steps.stack]]
name = "b.y"
value = "20"

[[visualizer.steps.stack]]
name = "b.label"
value = '"Alpha"'

[[visualizer.steps]]
arrows = []
explanation = "Modifier b.x et b.label n'affecte pas a — ce sont des copies indépendantes. C'est une copie profonde car label est un tableau (pas un pointeur). Si c'était char*, la copie serait shallow."
heap = []
label = "Après modification de b"
step_label = "Après modif — a inchangé, b modifié"

[[visualizer.steps.call_frames]]
args = "void"
function_name = "main"
is_active = true

[[visualizer.steps.stack]]
name = "a.x"
value = "10"

[[visualizer.steps.stack]]
name = "a.y"
value = "20"

[[visualizer.steps.stack]]
name = "a.label"
value = '"Alpha"'

[[visualizer.steps.stack]]
name = "b.x"
value = "99"

[[visualizer.steps.stack]]
name = "b.y"
value = "20"

[[visualizer.steps.stack]]
name = "b.label"
value = '"Beta"'
